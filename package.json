{
  "name": "gold_digger",
  "version": "0.2.6",
  "description": "MySQL/MariaDB query automation tool with structured output",
  "private": true,
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "format": "prettier --write --config .prettierrc.json",
    "format:check": "prettier --check --config .prettierrc.json"
  }
}
